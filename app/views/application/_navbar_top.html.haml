-#=image_tag ("back_up.png")

%nav.navbar.navbar-inverse.hidden-print#top-bar
  .container-fluid
    .navbar-header
      .navbar-brand
        = link_to root_path do
          =fa_icon 'stethoscope 3x'
          -#= image_tag 'logo.png', alt: t('app.company.short_name')
          = t('app.name')
          +7(495)321-32-21
          -#.phone
          -#  +7-495-321-32-21

      %button.navbar-toggle.collapsed{type: 'button', data: {toggle: 'collapse', target: '#bs-example-navbar-collapse-1'}}
        %span.sr-only
          Toggle navigation
        %span.icon-bar
        %span.icon-bar
        -#%span.icon-bar

    -#%ul.nav.navbar-nav.nav-center
    -#  .main-time-portion
    -#    gggs
    -#%ul.nav.navbar-nav
    -#  %li +7-495-321-32-21
    -#%p=@time #{font-color: 'white'}


    %ul.nav.navbar-nav.navbar-right.collapse.navbar-collapse#bs-example-navbar-collapse-1
      -#%li= t('app.name')
      -#%p +7-495-321-32-21

      %li= link_to "Все товары", products_path, {class: 'butto'}
      %li= link_to "Корзина", line_items_path, {class: 'butto'}
      %li
      -#%li
      -#  - if @current_user.blank?
      -#    = link_to login_path do
      -#      = fa_icon 'sign-in' 'войти'
      -#  - else
      -#    = link_to @current_user do
      -#      = fa_icon 'user'
      -#      = t(@current_user.name)
      %li
      - if @current_user.blank?
        %li
          = link_to login_path, {class: 'butto' } do
            = fa_icon 'sign-in'
      - else
        %li
          = link_to @current_user, {class: 'butto' } do
            = fa_icon 'user'
            = t(@current_user.name)
        %li
          = link_to logout_path, {class: 'butto' } do
            = fa_icon 'sign-out'





